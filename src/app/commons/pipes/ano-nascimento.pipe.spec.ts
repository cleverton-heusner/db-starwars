import { AnoNascimentoPipe } from './ano-nascimento.pipe';

describe('AnoNascimentoPipe', () => {
  it('create an instance', () => {
    const pipe = new AnoNascimentoPipe();
    expect(pipe).toBeTruthy();
  });
});
